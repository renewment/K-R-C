/*
 * Exercise 2-6. Write a function setbits(x,p,n,y) that returns x with the n
 * bits that begin at position p inverted (i.e., 1 changed into o0 and vice
 * versa), leaving the other bits unchanged.
 */

#include <stdio.h>
